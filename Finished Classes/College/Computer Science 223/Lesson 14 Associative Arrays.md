---
tags: CPT_S_223 Computer_Science
---

Alternative name for map is *associative array*.

**Hash Function**

**Hash Collision**

To prevent hash collisions, we usually want hash table sizes to be prime. We also want the load factor ${\lambda}$ (or how much of the table is filled) to be under ${0.7}$.

**Rehash**